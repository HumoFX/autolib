import { ISendQueue } from '../interface';
import BaseSendQueue from './base';
export declare class FifoQueue extends BaseSendQueue implements ISendQueue {
    readonly queue: string[];
    constructor(sendNow?: (bytes: string) => number, canSend?: () => boolean);
    send(bytes: string): number;
    queueMessage(bytes: string): boolean;
    processQueue(): number;
}
export default FifoQueue;
